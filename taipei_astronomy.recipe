import os
import time
import re
import datetime

os.environ['TZ'] = 'Asia/Shanghai'
time.tzset()

class AdvancedUserRecipeTechNews(BasicNewsRecipe):
    title = datetime.datetime.now().strftime('科技新报 - %Y-%m-%d %H-%M')
    language = 'zh_CN'
    oldest_article = 1
    max_articles_per_feed = 15

    no_summaries = False
    use_embedded_content = False
    auto_cleanup = True

    remove_tags = [
        dict(name='hr'),
        dict(name='script'),
        dict(name='style'),
        dict(name='aside'),
        dict(name='footer'),
        dict(name='nav'),
        dict(name='div', attrs={'class': ['tag','tags','category','sidebar','ad','related','share','entry-meta']}),
        dict(name='span', attrs={'class': ['tag','tags']}),
    ]

    keep_only_tags = [
        dict(name='h1'),
        dict(name='div', attrs={'class': 'entry-content'}),
    ]

    def preprocess_html(self, soup):
        for a in soup.find_all('a'):
            a.decompose()
        from calibre.utils.chinese_convert import t2s
        for txt in soup.find_all(text=True):
            if txt.parent.name not in ('script','style') and txt.strip():
                txt.replace_with(t2s(txt))
        return soup

    feeds = [
        ('科技新报全站', 'https://technews.tw/feed'),
    ]
